#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#include "move.h"
#include "board.h"


int main() {


    // init_board();
    // output_board();
    // int address;
    // Move move;
    // for (;;) {
    //     printf("input move: ");

    //     char line[8];
    //     fgets(line, sizeof(line), stdin);
    //     if (strncmp(line, "undo", 4) == 0) {
    //         unmake_move(move);
    //     } else {
    //         move = process_move(line);
    //         make_move(move);
    //     }
    //     output_board();
    // }
}